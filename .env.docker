# Docker Environment Variables
# Copy this file to .env and fill in your actual values

# WorkOS Configuration
WORKOS_API_KEY=sk_your_api_key_here
WORKOS_CLIENT_ID=client_your_client_id_here
WORKOS_REDIRECT_URI=http://localhost:3000/auth/callback
WORKOS_COOKIE_PASSWORD=32-character-minimum-secret-key-change-this

# JWT Configuration
SIMPLEJWT_SIGNING_KEY=your-jwt-signing-key-change-in-production

# Database (PostgreSQL is used in Docker)
# DATABASE_URL is automatically configured in docker-compose.yml

# Optional: Override default ports
# FRONTEND_PORT=3000
# BACKEND_PORT=8000
# DB_PORT=5432